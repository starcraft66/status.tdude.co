{
  "name": "tdude.co_status",
  "description": "Status Page For All tdude.co Services",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "statusfy dev",
    "build": "statusfy build",
    "start": "statusfy start",
    "generate": "statusfy generate",
    "new-incident": "statusfy new-incident"
  },
  "dependencies": {
    "statusfy": "0.3.0-alpha.2"
  },
  "engines": {
    "node": ">=8.10",
    "npm": ">=5"
  }
}
